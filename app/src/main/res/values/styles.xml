<resources>

    <!-- App theme using Material Components -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Style for activities without action bar -->
    <style name="AppTheme.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
    <resources>

        <!-- App theme using Material Components -->
        <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
            <!-- Customize your theme here -->
            <item name="colorPrimary">@color/colorPrimary</item>
            <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
            <item name="colorAccent">@color/colorAccent</item>
            <item name="colorSurface">@color/colorBackground</item> <!-- Surface color -->
            <item name="colorBackground">@color/colorBackground</item> <!-- Background color -->
            <item name="colorError">@color/colorError</item> <!-- Error color -->
            <item name="android:textColorPrimary">@color/colorOnBackground</item> <!-- Primary text color -->
            <item name="android:textColorSecondary">@color/colorOnSurface</item> <!-- Secondary text color -->
        </style>

        <!-- Style for activities without action bar -->
        <style name="AppTheme.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
            <item name="windowActionBar">false</item>
            <item name="windowNoTitle">true</item>
            <item name="android:statusBarColor">@android:color/transparent</item>
        </style>

        <!-- AppBar style (overlay for action bar) -->
        <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

        <!-- Popup overlay style -->
        <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    </resources>

    <!-- AppBar style (overlay for action bar) -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <!-- Popup overlay style -->
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

</resources>

