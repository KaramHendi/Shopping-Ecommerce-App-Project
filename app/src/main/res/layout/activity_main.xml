<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/mainContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:background="@drawable/appbackground">

    <!-- Logo -->
    <ImageView
        android:id="@+id/imgLogo"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:src="@drawable/applogo_"
        android:layout_marginBottom="32dp"
        android:contentDescription="SmartKart Logo" />

    <!-- Welcome Text -->
    <TextView
        android:id="@+id/textViewOnboardWelcomeHeader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to SmartKart"
        android:textSize="26sp"
        android:textStyle="bold"
        android:textColor="#212121"
        android:gravity="center"
        android:layout_marginBottom="16dp"
        android:lineSpacingExtra="4dp" />

    <!-- Subtitle -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Shop smart, shop easy!"
        android:textSize="16sp"
        android:textColor="#757575"
        android:gravity="center"
        android:layout_marginBottom="40dp" />

    <!-- Get Started Button -->
    <Button
        android:id="@+id/btnMainGetStarted"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Get Started"
        android:textAllCaps="false"
        android:backgroundTint="@color/primaryColor"
        android:textColor="@android:color/white"
        android:paddingVertical="14dp"
        android:layout_marginTop="16dp"
        android:elevation="8dp"
        android:background="@drawable/rounded_button"
        android:layout_gravity="center"/>

</LinearLayout>
